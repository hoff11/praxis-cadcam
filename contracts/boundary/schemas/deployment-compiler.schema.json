{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://praxis.cadcam/schemas/deployment-compiler/v1.0.0",
  "title": "Deployment Compiler",
  "description": "Frozen governed compile context used repeatedly for production translation. The three fields deploymentCompilerId, deploymentCompilerVersion, and digest map directly to deploymentCompilerRef.id, deploymentCompilerRef.version, and deploymentCompilerRef.digest in governed compile requests.",
  "type": "object",
  "required": [
    "schemaVersion",
    "deploymentCompilerId",
    "deploymentCompilerVersion",
    "digest",
    "machineDeploymentRef",
    "capabilityProfileRef",
    "policyPackRef",
    "controllerBaseRef",
    "compilerPins",
    "frozen"
  ],
  "properties": {
    "schemaVersion": {
      "type": "string",
      "const": "1.0.0",
      "description": "Schema version for this deployment-compiler artifact. Required and fixed to keep digest projection stable."
    },
    "deploymentCompilerId": {
      "type": "string",
      "pattern": "^[a-z0-9\\-]+$",
      "description": "Canonical identity for this deployment compiler. Maps to deploymentCompilerRef.id in governed compile requests."
    },
    "deploymentCompilerVersion": {
      "type": "string",
      "pattern": "^\\d+\\.\\d+\\.\\d+$",
      "description": "Version of this deployment compiler artifact. Maps to deploymentCompilerRef.version in governed compile requests."
    },
    "digest": {
      "type": "string",
      "pattern": "^sha256:[a-f0-9]{64}$",
      "description": "Canonical digest of the frozen deployment-compiler semantic projection. Maps to deploymentCompilerRef.digest in governed compile requests. Format: sha256:<64 lowercase hex>. Included projection fields: deploymentCompilerId, deploymentCompilerVersion, schemaVersion, machineDeploymentRef(deploymentId/deploymentVersion), capabilityProfileRef(capabilityId/capabilityVersion plus optional digest/checksum/hash), controllerBaseRef(controllerBaseId/controllerBaseVersion plus optional digest/checksum/hash), policyPackRef(policyPackId/policyPackVersion plus optional digest/checksum/hash), compilerPins(compilerVersion/compilerBuild/irVersion/emitterVersion), compilerRequestMapping(machineCapabilityId if present and targetSelection layered id/version refs), frozen(isFrozen). Excluded from projection: frozenAt, frozenBy, trustedOverrides, publication wrappers, timestamps, and non-semantic metadata."
    },
    "machineDeploymentRef": {
      "type": "object",
      "required": ["deploymentId", "deploymentVersion"],
      "properties": {
        "deploymentId": {
          "type": "string",
          "pattern": "^[a-z0-9\\-]+$"
        },
        "deploymentVersion": {
          "type": "string",
          "pattern": "^\\d+\\.\\d+\\.\\d+$"
        }
      },
      "additionalProperties": false
    },
    "capabilityProfileRef": {
      "type": "object",
      "required": ["capabilityId", "capabilityVersion"],
      "properties": {
        "capabilityId": {
          "type": "string",
          "pattern": "^[a-z0-9\\-]+$"
        },
        "capabilityVersion": {
          "type": "string",
          "pattern": "^\\d+\\.\\d+\\.\\d+$"
        }
      },
      "additionalProperties": false
    },
    "policyPackRef": {
      "type": "object",
      "required": ["policyPackId", "policyPackVersion"],
      "properties": {
        "policyPackId": {
          "type": "string",
          "pattern": "^[a-z0-9\\-]+$"
        },
        "policyPackVersion": {
          "type": "string",
          "pattern": "^\\d+\\.\\d+\\.\\d+$"
        }
      },
      "additionalProperties": false
    },
    "controllerBaseRef": {
      "type": "object",
      "required": ["controllerBaseId", "controllerBaseVersion"],
      "properties": {
        "controllerBaseId": {
          "type": "string",
          "pattern": "^[a-z0-9\\-]+$"
        },
        "controllerBaseVersion": {
          "type": "string",
          "pattern": "^\\d+\\.\\d+\\.\\d+$"
        }
      },
      "additionalProperties": false
    },
    "compilerPins": {
      "type": "object",
      "required": ["compilerVersion", "compilerBuild", "irVersion", "emitterVersion"],
      "properties": {
        "compilerVersion": {"type": "string"},
        "compilerBuild": {"type": "string"},
        "irVersion": {"type": "string"},
        "emitterVersion": {"type": "string"}
      },
      "additionalProperties": false
    },
    "trustedOverrides": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["overrideClass"],
        "properties": {
          "overrideClass": {"type": "string"},
          "value": {
            "type": ["string", "number", "boolean", "object", "array", "null"]
          },
          "approvedBy": {"type": "string"},
          "approvalRef": {"type": "string"}
        },
        "additionalProperties": false
      }
    },
    "compilerRequestMapping": {
      "type": "object",
      "description": "Governed compile request target echo. Contains only targetSelection — machineCapabilityId is forbidden in governed mode and must not appear here.",
      "required": ["targetSelection"],
      "properties": {
        "targetSelection": {
          "type": "object",
          "required": ["machineProfile", "policyPack", "controllerBase"],
          "properties": {
            "machineProfile": {
              "type": "object",
              "required": ["id", "version"],
              "properties": {
                "id": {"type": "string", "pattern": "^[a-z0-9\\-]+$"},
                "version": {"type": "string", "pattern": "^\\d+\\.\\d+\\.\\d+$"}
              },
              "additionalProperties": false
            },
            "policyPack": {
              "type": "object",
              "required": ["id", "version"],
              "properties": {
                "id": {"type": "string", "pattern": "^[a-z0-9\\-]+$"},
                "version": {"type": "string", "pattern": "^\\d+\\.\\d+\\.\\d+$"}
              },
              "additionalProperties": false
            },
            "controllerBase": {
              "type": "object",
              "required": ["id", "version"],
              "properties": {
                "id": {"type": "string", "pattern": "^[a-z0-9\\-]+$"},
                "version": {"type": "string", "pattern": "^\\d+\\.\\d+\\.\\d+$"}
              },
              "additionalProperties": false
            }
          },
          "additionalProperties": false
        }
      },
      "additionalProperties": false
    },
    "frozen": {
      "type": "object",
      "required": ["isFrozen", "frozenAt"],
      "description": "Freeze-state metadata. Digest is valid only for frozen artifacts and must be computed from projection containing frozen.isFrozen only.",
      "properties": {
        "isFrozen": {"type": "boolean", "const": true},
        "frozenAt": {"type": "string", "format": "date-time"},
        "frozenBy": {"type": "string"}
      },
      "additionalProperties": false
    }
  },
  "additionalProperties": false
}
